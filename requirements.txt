# Python deps for the DAQ host scripts (scope, captures, diagnostics).
#   pip install -r requirements.txt
#
# GUI scope (scripts/dac_scope_qt.py) uses PyQtGraph on a Qt5 backend.
# Run setup_gui_env.ps1 to create the isolated, supported Qt 5.15 runtime.

numpy>=1.20
pyserial>=3.4
matplotlib>=3.3
PyQt5>=5.15,<5.16
pyqtgraph>=0.13.7,<0.14
